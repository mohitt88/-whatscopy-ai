/** @type {import('next').NextConfig} */
const nextConfig = { reactStrictMode: true }
module.exports = nextConfig
```
Commit ✅

---

**File 3:** Click "Add file" → "Create new file" → type exactly:
```
pages/api/generate.js
